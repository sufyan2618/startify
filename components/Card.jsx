import React from 'react'

const Card = ({post}) => {
    // Assuming post is an object with properties like title, author, date, etc.
    const { title, author, date, category, description, image } = post;
    
  return (
    <div>Card</div>
  )
}

export default Card